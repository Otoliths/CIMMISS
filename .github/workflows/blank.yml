name: R-CMD-check

on:
  push:
    branches:
      - master
    schedule:
      - cron: '*/1 * * * *'
  pull_request:
    branches:
      - master

jobs:
  bot:
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout codes'
        uses: actions/checkout@v1
      - name: 'Get CIMMISS'
        run: bash ./code.sh
